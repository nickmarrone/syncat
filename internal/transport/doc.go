// Package transport abstracts peer connectivity behind a Transport interface, with a tailcat implementation for production and an in-memory pipe implementation for tests (SPEC.md §10).
package transport
